Librerias a instalar(dentro del entorno virtual de CADA CONTENEDOR):

pip install jsonrpcserver
pip install requests


comandos utiles:
  -activar entornos virtuales: source env/bin/activate
  -crear entornos virtuales: python3 -m venv env
  - instalar pip y los entornos virtuales de python -> apt install -y python3 python3-pip python3-venv
  (Nota: si arroja un error hay que hacer un apt update y volve a ejecutar ese comando)

  verificar que pip y python esten instalados

  python3 --version
  pip3 --version




comandos utiles docker:
  -crear un nuevo contenedor -> docker run -dit --name transporte -v ~/Documents/talleresGrandes/taller_tiendaMiddle/transporte/:/opt/transporte/ --network tienda_middle --ip=192.168.1.7 juancho926/customubuntu:2.1
  (se cambia el nombre, la ip y el volumen, se usa la misma red)

  - Correr contenedor: docker exec -it transporte bash
  - Eliminar un contenedor: docker rm -f


correr middleWare para ventas

curl -X POST http://192.168.1.10:5010 \
-H "Content-Type: application/json" \
-d '{"jsonrpc":"2.0","method":"middleWareController","params":{},"id":1}'


correr middlware comprasProveedores
curl -X POST http://192.168.1.10:5010   -H "Content-Type: application/json"   -d '{
    "jsonrpc": "2.0",
    "method": "middlewareControllerProveedores",
    "params": {
      "origen": "proveedores"
    },
    "id": 1
  }'


correr la documentación usando pydoc:

  pdoc --http 0.0.0.0:8080 \
     taller_tiendaMiddle.comprasVentas \
     taller_tiendaMiddle.inventario \
     taller_tiendaMiddle.middleWare \
     taller_tiendaMiddle.proveedores \
     taller_tiendaMiddle.tienda


  se debe de copiar todo esto